{%- comment -%}
Placeholder for blocks/feature_row.html so local builds don’t fail.
Replace with your real include later (or switch to a theme that provides it).
Accepted params (ignored here): features, title, intro
{%- endcomment -%}
<div class="feature-row">
  {%- if include.title -%}<h3>{{ include.title }}</h3>{%- endif -%}
  {%- if include.intro -%}<p>{{ include.intro }}</p>{%- endif -%}
  {%- if include.features and include.features.size > 0 -%}
    <ul class="features">
      {%- for f in include.features -%}
        <li>
          {%- if f.url -%}<a href="{{ f.url }}">{%- endif -%}
          {{ f.title | default: f.text | default: "Feature" }}
          {%- if f.url -%}</a>{%- endif -%}
        </li>
      {%- endfor -%}
    </ul>
  {%- endif -%}
</div>
