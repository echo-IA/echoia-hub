#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
[ -x ./bin/jekyll ] || ./bin/setup
export JEKYLL_GITHUB_TOKEN="${JEKYLL_GITHUB_TOKEN:-dummy}"
exec ./bin/jekyll serve --livereload "$@"
